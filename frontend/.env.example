# Frontend server runtime environment
# These values are read by server.js through process.env and are not exposed to browser code.

# Dev-only auth bypass. Active only when NODE_ENV is not "production"; ignored in prod.
# Lets the app run locally without the auth proxy that normally injects access-token-* headers.
# DEV_AUTH_UPN must be a @bnl.gov address; DEV_AUTH_ROLES must include a recognized role
# (ios.operator, ios.admin, skybeam.admin). Use ios.admin to reach admin routes.
DEV_AUTH_UPN=
DEV_AUTH_NAME=
DEV_AUTH_ROLES=

# Optional: override the Express server port.
# Defaults to 5173 for HTTP and 443 when SSL_CERT_PATH/SSL_KEY_PATH are set.
PORT=

# Optional: enable HTTPS directly in the frontend server.
# Production HAProxy backend SSL expects the app to be reachable over HTTPS.
SSL_CERT_PATH=
SSL_KEY_PATH=

# Backend service targets used by the frontend server proxy.
# These are server-side URLs and should not use the VITE_ prefix.
PRESETS_TARGET=http://localhost:8005
CONFIG_TARGET=http://localhost:8004
CONTROL_TARGET=http://localhost:8003
TILED_TARGET=http://localhost:8000
QUEUESERVER_TARGET=http://localhost:60610
TILED_API_KEY=
QSERVER_API_KEY=

# Finch receives same-origin URLs from ClientFinchBridge; no VITE_* service URLs
# are needed for normal development or production.